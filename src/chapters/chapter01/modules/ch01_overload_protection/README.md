# 第一章 07 过载保护

参考第一章第 113 页“热继电器的应用举例”。模块实现启动/停止与自锁、FR1 过载动作、控制常闭触点断开、KM1 释放、电动机故障停止、FR1 手动复位，以及“复位不自动重启”的安全语义。

模块全部代码位于本目录，未修改平台公共层、Registry、Schema、`index.html` 或第二章成熟电路。正式导航接入由平台维护者按依赖顺序加载 `circuit.data.js`、`runtime.js`、`facade.js`、`module.js` 后注册定义。
